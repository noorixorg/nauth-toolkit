/**
 * Tokens unit tests
 */
import { NAUTH_CLIENT_CONFIG } from './tokens';

describe('NAUTH_CLIENT_CONFIG', () => {
  it('should be defined', () => {
    expect(NAUTH_CLIENT_CONFIG).toBeDefined();
  });
});
