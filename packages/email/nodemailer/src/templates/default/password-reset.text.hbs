Password Reset

{{#if fullName}}Hi {{fullName}},{{/if}}
{{#if firstName}}Hi {{firstName}},{{/if}}
{{#if lastName}}Hi {{lastName}},{{/if}}
{{#if userName}}Hi {{userName}},{{/if}}

We received a request to reset your password.{{#if link}}
  Use the link below to create a new password:

  {{link}}
{{else}}
  Use the code below to reset your password:

  {{code}}
{{/if}}

{{#if expiryMinutes}}{{#if link}}This link expires in {{expiryMinutes}} minutes.{{else}}This code expires in
    {{expiryMinutes}}
    minutes.{{/if}}{{/if}}

If you didn't request a password reset, your account is secure and you can ignore this email.
