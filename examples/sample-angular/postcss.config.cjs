/**
 * PostCSS configuration for Tailwind CSS v4.
 *
 * Angular CLI will pick this up automatically.
 * The @tailwindcss/postcss plugin enables @import 'tailwindcss'
 * and @apply directives used in our component styles.
 */
module.exports = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
